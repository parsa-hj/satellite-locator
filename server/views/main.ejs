<!DOCTYPE html>
<html>
  <head>
    <title>ISS Location Tracker</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/styles.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= GOOGLE_MAPS_API_KEY %>&callback=initMap"></script>
    <script src="/map.js"></script>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>ISS Location Tracker</h1>
        <p class="text-light text-center mb-4">
          Track the International Space Station in real-time
        </p>
      </div>

      <div class="center-container">
        <button type="button" class="btn" id="showButton">
          <span class="me-2">🚀</span> Track ISS Location
        </button>
      </div>

      <div class="container text-center hidden long-lat">
        <div class="row g-4">
          <div class="col-md-6">
            <div class="shadow">
              <h4>Latitude</h4>
              <p class="text-light mb-0"><%= lat %>°</p>
            </div>
          </div>
          <div class="col-md-6">
            <div class="shadow">
              <h4>Longitude</h4>
              <p class="text-light mb-0"><%= long %>°</p>
            </div>
          </div>
        </div>
        <div class="row mt-4">
          <div class="col-12">
            <div class="shadow">
              <h4>Current Coordinates</h4>
              <p class="text-light mb-0"><%= lat %>, <%= long %></p>
            </div>
          </div>
        </div>
      </div>

      <div class="google-map hidden">
        <div id="map" data-lat="<%= lat %>" data-long="<%= long %>"></div>
      </div>
    </div>
  </body>
</html>
